import {EndpointValidator} from "../../src/app/Service/Impl";

describe('Module: Api', () => {
    describe('Class: EndpointValidator', () => {

        describe('After Instantiation', () => {
            let subject:EndpointValidator;

            beforeEach(() => {
                subject = new EndpointValidator();
            });

            describe('Method: ', () => {

            });
        });

    });
});
